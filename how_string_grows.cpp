#include<bits/stdc++.h>
using namespace std;

int main()
{
    string s = "bbbbbbb";
    cout<<s<<" "<<s.size()<<" "<<s.capacity()<<endl;
    
    for(int i = 0; i<4; i++ )
        s = s+"a";
    cout<<s<<" "<<s.size()<<" "<<s.capacity()<<endl;

    for(int i = 0; i<4; i++ )
        s = s+"a";
    cout<<s<<" "<<s.size()<<" "<<s.capacity()<<endl;

    for(int i = 0; i<4; i++ )
        s = s+"a";
    cout<<s<<" "<<s.size()<<" "<<s.capacity()<<endl;

    for(int i = 0; i<4; i++ )
        s = s+"a";
    cout<<s<<" "<<s.size()<<" "<<s.capacity()<<endl;

    for(int i = 0; i<4; i++ )
        s = s+"a";
    cout<<s<<" "<<s.size()<<" "<<s.capacity()<<endl;


}